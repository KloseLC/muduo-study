什么是左值,什么是右值?

  表达式的左边变量称之为左值,右侧为右值
左值接受复制,右值用来给左值复制的临时值,如:
 a = 1
一般代码里,右值为临时存储,那么用完即扔.
其中包括赋值copy 内存回收等操作,性能开销较大


引用常用在非临时变量左值,C++11之后可以用到临时变量的引用

左值引用为T&,右值引用为T&&  
这是在函数形参上的表示


右值引用跳过了赋值copy和内存回收的操作,直接把右值的内存赋于左值,也称之为move操作

C++11的std::move就是这个含义


实现参数的完美传递和转移语义
